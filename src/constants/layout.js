// Shared layout width and padding for desktop/content container
// Use this inside full-width section backgrounds to constrain inner content
export const LAYOUT = "max-w-6xl mx-auto px-4 sm:px-6 lg:px-8";

// Optional: export a numeric pixel value (if you need to calc styles in JS)
export const DESKTOP_MAX_WIDTH_PX = 1152; // corresponds roughly to max-w-6xl

// Padding-only variant: no fixed max-width, just symmetric horizontal padding
export const PADDING = "w-full px-6 lg:px-12";
